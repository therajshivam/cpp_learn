// Array

//  - declaration 
//      - a data struct that can hold multiple vals
//      - values are accessed by an index num
//      - it is a kind of variable that holds multiple values
//      - values can be of a single data type which is declared


//  - sizeof() operator
//      - determines the size in byes of a:
//      - variable, data type, class, objects, etc.
//      - sizeof(grades)/sizeof(grades[0]) or sizeof(grades)/sizeof(char)

//  - Iterate over an array
//      - using the for loop

//  - foreach loop
//      - loop that eases the traversal over an iterable data set
//      - good for displaying elmts of array as it is less flexible

//  - Pass array to a function
//      - 
