#include<iostream>
using namespace std;

int main() {

    // unsigned : only unsigned (only +)
    // signed : both signed and unsigned (- & +)
    // if negative int is given in unsigned then it will give garbage value; random values
    unsigned smallTeaPack = 1200;

    long largeTeaStorage = 1000000;

    long long largestTeaStorage = 100000000000;

    short teaSample = 25;

    cout << largeTeaStorage << endl;

    return 0;
}