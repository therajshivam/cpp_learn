#include <iostream>
#include <cmath>
using namespace std;

int main() {

    double x =3.14;
    double y =4;
    double z;

    // z = max(x, y); //4
    // z = min(x,y); //3

    // z = pow(2, 3); //power
    // z = sqrt(9); //square root

    // z = abs(-33); // absolute will give positive value of the neg number

    // z = round(x); // rounds the num
    // z = ceil(x); // rounds up the val
    // z = floor(3.99); // rounds down the val

    

    cout<< z;

    return 0;
}