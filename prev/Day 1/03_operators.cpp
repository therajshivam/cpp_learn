
#include<iostream>
using namespace std;

int main() {

    // 1. arithmetic operator (+,-,*,/,%)

    int a = 2/5;
    cout << a << endl; 
    // int/int = int

    // therefore we use float or double
    // float/int = float
    // double/int = double

    float b = 2.0/5;
    cout << b << endl;

    // 2. relational operator (=,<,>,<=,>=,!=,==)

    int i = 2;
    int j = 3;

    bool first = (a==b);
    cout << first << endl;

    bool second = (a>b);
    cout << second << endl;

    bool third = (a<=b);
    cout << third << endl;

    bool fourth = (a!=b);
    cout << fourth << endl;

    // = is assingment operator as it assigns the value (eg . int a = 3;)

    // 3. logical operator (&& (AND), || (OR), ! (NOT))

    // 4. Bitwise operator
}
